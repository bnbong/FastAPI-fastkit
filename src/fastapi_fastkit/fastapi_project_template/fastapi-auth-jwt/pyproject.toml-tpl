[project]
name = "<project_name>"
version = "0.1.0"
description = "[FastAPI-fastkit templated] <description>"
authors = [
    {name = "<author>", email = "<author_email>"},
]
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.4",
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "email-validator>=2.3.0",
    "python-dotenv>=1.2.3",
    "python-multipart>=0.0.32",
    "sqlmodel>=0.0.42",
    "SQLAlchemy>=2.0.52",
    "alembic>=1.19.1",
    "PyJWT>=2.13.0",
    "pwdlib[argon2]>=0.3.1",
    "psycopg[binary]>=3.3.5",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "httpx>=0.28.1",
    "black>=26.5.1",
    "isort>=9.0.1",
    "mypy>=2.3.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "httpx>=0.28.1",
    "black>=26.5.1",
    "isort>=9.0.1",
    "mypy>=2.3.1",
]

[tool.fastapi-fastkit]
managed = true
template = "fastapi-auth-jwt"
package_manager = "uv"
app_module = "src.app.main:app"
features = ["authentication:JWT"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.black]
line-length = 88
target-version = ["py312"]
extend-exclude = "src/app/alembic/versions"

[tool.isort]
profile = "black"
line_length = 88
known_first_party = ["src"]
skip_glob = ["src/app/alembic/versions/*"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
exclude = "src/app/alembic/versions"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "strict"
addopts = "-v --tb=short"
